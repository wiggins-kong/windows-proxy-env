# 更新日志

本项目使用语义化版本号，发布日期使用本地日期。

## [1.1.0] - 2026-09-12

### 新增

- HTTP(S) 与 SOCKS 双通道：两个通道可独立启用，也可同时启用。
- 左右分栏界面：左侧通道配置，右侧「将写入的变量 / 应用配置 / 当前用户环境变量」。
- SOCKS 复用 HTTP(S) 的地址与认证；HTTP(S) 关闭后自动展开为独立地址。
- 连通性测试分通道显示结果；托盘「测试连通性」一次覆盖所有已启用通道。
- 静默启动：随 Windows 启动后隐藏主窗口，仅驻留系统托盘。
- 两个通道都关掉后点「应用更改」等同停用代理，并把「通道已关闭」写回配置，重启后界面与实际一致。

### 改进

- 更换应用与托盘图标：概念 C「轨道路由」——靛蓝对角渐变底 + 双交叉轨道 + 中央玻璃核心，标题栏、任务栏、托盘与 Windows/Android/iOS 图标统一由同一几何生成。
- 标题栏改用独立 SVG logo（`ui/assets/proxyenv-logo.svg`，与图标脚本几何一致）。
- 托盘 tooltip 分列 HTTP(S) 与 SOCKS 两个通道的地址。

### 变更

- 配置模型改为双通道，旧版单通道 `config.json` 首次读取时自动迁移。
- SOCKS 类型收敛为 SOCKS5 / SOCKS5h，移除 SOCKS4 / SOCKS4a。
- 主题固定跟随系统，移除手动三态切换。
- 设置中移除字体选择，界面字体固定为微软雅黑，仅保留字号调节。
- 「应用配置」三个按钮统一 40px 高（收敛掉原先 38/40/42 三条互相打架的规则），按钮行宽度固定，不再随状态文案变化。
- 「测试通道」按钮图标改为闪电。
- 托盘图标去掉绿点/灰点角标，改为彩色（启用）/ 灰度（停用）区分。
- 窗口默认尺寸调整为 1040×780，最小 1000×640，并去掉窄屏单栏折叠，始终保留左右两栏。
- 图标（卡片标题、说明、设置按钮）改为 `currentColor` 描边渲染，修复深色模式下显示为黑色不可见的问题。
- 说明行图标与首行文字光学对齐。

### 修复

- 写入前先清理旧代理变量，避免切换通道后残留上一套配置。
- 修复「应用配置」按钮宽窄随状态文案跳变（`.action-copy` 隐式网格列按 max-content 撑开）。
- 修复演示页 `demo/two-column.html` 里 `state.testPanel` 拼写错误导致通道开关点击后无响应。
- 修复任务栏图标模糊：Tauri 把 `.ico` 的 `entries()[0]` 当作窗口图标（任务栏/悬停预览/Alt-Tab 都用它），而 PIL 保存 ICO 时按尺寸升序排列、第一项必然是 16px；改为手动按序写 ICO（第一项放 128px 简化版），窗口图标从 16px 变为 128px。16–48px 各层与托盘同时改用简化画法（加粗交叉轨道 + 实心核心），大尺寸仍用细节版。

## [1.0.1] - 2026-09-10

### 新增

- 启用代理后修改配置时显示“配置已更改”横幅，可一键应用当前表单值。
- 托盘图标随代理状态切换，启用为彩色绿点，停用为灰度灰点。
- 托盘 tooltip 显示当前状态与代理地址，右键菜单按状态自动置灰。
- 增加 `changelog.md` 和 `development.md`，拆分版本记录与开发说明。

### 改进

- 托盘图标整体放大 20%，提升小尺寸下的辨识度。
- GitHub Actions 发布时自动读取 `changelog.md` 作为 release note。
- 构建前强制刷新前端嵌入资源，避免 `ui/` 更新后发布旧界面。
- 图标生成脚本改为基于项目相对路径，可在任意工作目录运行。
- 构建脚本移除机器专用 Python 路径，改为通用解释器探测。
- 清理开发工具工作区及旧绝对路径，统一项目目录和文档结构。

### 修复

- 修复 `build.sh` 在系统缺少 `brotli` 模块时中断、未复制 `dist` 的问题。
- 修复启用状态下配置变化不直观、容易误以为已生效的问题。

## [1.0.0] - 2026-08-30

### 新增

- 首次发布便携单文件 Windows 代理环境变量开关。
- 支持 HTTP、HTTPS、SOCKS4、SOCKS4a、SOCKS5、SOCKS5h。
- 支持代理认证、`NO_PROXY`、高级变量模式和连通性测试。
- 支持一键启用、停用、清除及新终端即时生效。
- 支持托盘常驻、开机自启、单实例保护和状态展示。
- 支持跟随系统、浅色、深色主题，以及字体和字号设置。
- 增加 Windows GitHub Actions 发布工作流。
