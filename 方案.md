# ProxyEnv — 便携代理环境变量开关工具 · 方案

## 一、定位

单文件便携 EXE，Win10/11 双击即用。管理**用户级**代理环境变量
（HTTP_PROXY / HTTPS_PROXY / ALL_PROXY / NO_PROXY），UI 贴合
Windows 11 设计风格（Mica 背景、亚克力、深浅色跟随系统）。

## 二、技术栈（已定：Tauri v2）

| 项目 | 选型 |
|------|------|
| 框架 | Tauri v2（Rust 1.98 + 原生 Web 前端） |
| 产物 | 单 EXE，约 8–15MB，便携 |
| 运行时 | WebView2（Win10/11 自带 Edge 均有，免安装） |
| 原生能力 | window-effects（Mica/亚克力）、系统托盘、注册表读写、reqwest 网络测试 |

## 三、核心功能（分支 2/3 定案）

### 1. 代理类型与变量映射
类型下拉：**HTTP / HTTPS / SOCKS4 / SOCKS4a / SOCKS5 / SOCKS5h**
- HTTP 系 → `HTTP_PROXY`、`HTTPS_PROXY` = `http://[user:pass@]host:port`
- SOCKS 系 → `ALL_PROXY` = `socks4|socks4a|socks5|socks5h://[user:pass@]host:port`

### 2. 设置项
- 代理地址、端口
- 认证代理：用户名 / 密码可选填（自动拼接 `user:pass@`）
- NO_PROXY：可编辑，默认 `localhost,127.0.0.1,::1`（含常见内网豁免）
- 高级模式（可折叠）：4 个变量手动覆盖编辑

### 3. 开关与状态
- 一键**启用**：写入 4 变量并广播刷新，新终端立即生效
- 一键**停用**：删除 4 变量，恢复无代理
- 一键**清除**：同上（保留 NO_PROXY 编辑值）
- **状态展示**：启动时读取当前用户级变量现值，逐条显示「已设置/未设置」

### 4. 增强
- 系统托盘常驻：菜单含 启用/停用/测试连通/显示主窗/退出
- 开机自启：开关（写 HKCU\Run），默认关
- 连通测试：reqwest 走当前代理请求 generate_204，显示 通/不通 + 延迟
- 多套配置：**不做**（已确认单一配置）

## 四、UI/UX（分支 4 定案，贴合 Win11）

- **Mica 窗口背景 + 亚克力效果**（Tauri window-effects 原生调用）
- 无边框自绘标题栏：圆角窗口、顶部拖拽区、自绘最小化/关闭
- **三态主题**：跟随系统（默认）/ 锁定浅色 / 锁定深色；CSS variables 全站适配，深色下背景自动变暗
- **单页卡片流**，约 420×620 可缩放，滚动布局：
  1. **状态卡**：大号启用开关 + 4 变量现值列表（徽标：已设置/未设置）
  2. **设置卡**：类型下拉、地址、端口、认证（可选）、NO_PROXY
  3. **高级卡**（折叠）：4 变量手动编辑
  4. **操作区**：启用 / 停用 / 一键清除 / 测试（结果徽标）
  5. **底部**：开机自启开关、关于
- 中文单语；Fluent 风格图标；控件 hover/active 动效

## 五、技术要点

| 项 | 方案 |
|----|------|
| 环境变量写入 | 注册表 `HKCU\Environment` + 广播 `WM_SETTINGCHANGE`，新终端/程序立即生效 |
| 配置持久化 | `%APPDATA%\ProxyEnv\config.json`（用户选择） |
| 连通测试 | reqwest + 代理配置，超时 5s，目标 `https://www.gstatic.com/generate_204` |
| 权限 | 仅操作 HKCU 自身范围，无需管理员 |

## 六、目录结构

```
E:\WorkBuddy\Proxy_Env\
├─ 方案.md
└─ ProxyEnv\
   ├─ src-tauri\            # Rust 侧
   │  ├─ src\
   │  │  ├─ main.rs         # 入口、Tauri 命令注册
   │  │  ├─ env_util.rs     # HKCU\Environment 读写 + 广播
   │  │  ├─ proxy_rules.rs  # 类型→变量映射、URL 拼接
   │  │  ├─ config.rs       # %APPDATA% 配置读写
   │  │  ├─ autostart.rs    # HKCU\Run 自启开关
   │  │  └─ test.rs         # 连通测试
   │  └─ tauri.conf.json
   └─ ui\                   # 前端
      ├─ index.html
      ├─ style.css          # 卡片、控件、动效
      ├─ theme.css          # 深浅色 CSS variables
      └─ app.js             # 交互、主题三态、托盘联动
```

## 七、里程碑

| 阶段 | 内容 | 预计 |
|------|------|------|
| M1 | 脚手架 + Mica 窗口 + 三态主题骨架 | 0.5 天 |
| M2 | 核心逻辑：注册表读写 / 规则映射 / 配置持久化 | 0.5 天 |
| M3 | UI 完整联动：卡片、开关、测试、高级模式 | 0.5–1 天 |
| M4 | 托盘 / 自启 / 单 EXE 发布 / 细节打磨 | 0.5 天 |

## 八、已知取舍（分支 5 定案）

- 不签名：首次运行有 SmartScreen「更多信息 → 仍要运行」，已确认接受
- 配置存 `%APPDATA%`：换机器不跟随（便携性打了折扣，按你的选择）
- Win10 上 Mica 自动回退纯色背景：效果降级，功能不受影响